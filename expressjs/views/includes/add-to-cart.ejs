<% if(isAuthenticated) { %>
<form action="/cart" method="POST">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
  <button ype="submit" class="btn">Add to Cart</button>
  <input type="hidden" name="productId" value="<%= product._id %>" />
</form>
<% } %>
