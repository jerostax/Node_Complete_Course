<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (products.length > 0) { %>
                <% for (product of products) { %>
                    <ul>
                        <li>    
                          <%= product.productData.title %> (Quantity : <%= product.qty %>)
                        </li>
                    </ul>
                <% } %>
            <% } else { %>
                <h1>No product in the Cart</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>